MAS contractNet {

	infrastructure: Centralised

	agents:
		client; // the CNP initiator
        server #3; // the participants (3) that offer a service
        refusingServer; // a participant that always refuses
        deadServer; // a participant that does not answer

	aslSourcePath:
		"src/main/asl";
}
